const main = d3.select('main');
main.append('h1').text('Spider Chart');

// map arbitrary values around a set of days
const days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"];